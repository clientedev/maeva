{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python migrate_db.py && gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --keep-alive 2 main:app",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "always"
  }
}